apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
data:
  postgresUsername: {{ .Values.secrets.mongoUsername | b64enc | quote }}
  postgresPassword: {{ .Values.secrets.mongoPassword | b64enc | quote }}